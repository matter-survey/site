parameters:
    schema_path: '%kernel.project_dir%/schema.sql'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'

    App\Service\DatabaseService:
        arguments:
            $schemaPath: '%schema_path%'
