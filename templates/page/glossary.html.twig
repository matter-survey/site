{% extends 'base.html.twig' %}

{% block title %}{{ 'meta_title'|trans({}, 'glossary') }}{% endblock %}

{% block og_title %}{{ 'meta_title'|trans({}, 'glossary') }}{% endblock %}
{% block og_description %}{{ 'meta_description'|trans({}, 'glossary') }}{% endblock %}

{% block extra_styles %}
    .glossary-content {
        max-width: 800px;
        margin: 0 auto;
    }

    .glossary-content h1 {
        font-size: 2rem;
        margin-bottom: 0.5rem;
        color: var(--gray-900);
    }

    .glossary-intro {
        color: var(--gray-500);
        margin-bottom: 2rem;
    }

    .glossary-nav {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        margin-bottom: 2rem;
        padding: 1rem;
        background: white;
        border: 1px solid var(--gray-200);
        border-radius: 0.5rem;
    }

    .glossary-nav a {
        padding: 0.25rem 0.75rem;
        background: var(--gray-100);
        border-radius: 0.25rem;
        text-decoration: none;
        color: var(--gray-700);
        font-weight: 500;
    }

    .glossary-nav a:hover {
        background: var(--primary);
        color: white;
    }

    .glossary-section {
        margin-bottom: 2.5rem;
    }

    .glossary-section h2 {
        font-size: 1.5rem;
        color: var(--primary);
        margin-bottom: 1rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid var(--primary);
    }

    .glossary-term {
        background: white;
        border: 1px solid var(--gray-200);
        border-radius: 0.5rem;
        padding: 1rem 1.25rem;
        margin-bottom: 1rem;
    }

    .glossary-term h3 {
        color: var(--gray-900);
        margin-bottom: 0.5rem;
        font-size: 1.1rem;
    }

    .glossary-term h3 code {
        background: var(--gray-100);
        padding: 0.125rem 0.5rem;
        border-radius: 0.25rem;
        font-size: 0.8rem;
        margin-left: 0.5rem;
        font-weight: normal;
    }

    .glossary-term p {
        color: var(--gray-600);
        line-height: 1.7;
        margin-bottom: 0.5rem;
    }

    .glossary-term p:last-child {
        margin-bottom: 0;
    }

    .glossary-term .example {
        background: var(--gray-50);
        padding: 0.75rem;
        border-radius: 0.375rem;
        margin-top: 0.75rem;
        font-size: 0.9rem;
    }

    .glossary-term .example strong {
        color: var(--gray-700);
    }

    .back-to-top {
        text-align: center;
        margin: 2rem 0;
    }

    .back-to-top a {
        color: var(--gray-500);
        text-decoration: none;
    }
{% endblock %}

{% block body %}
<div class="glossary-content">
    <h1>{{ 'title'|trans({}, 'glossary') }}</h1>
    <p class="glossary-intro">{{ 'intro'|trans({}, 'glossary') }}</p>

    <nav class="glossary-nav">
        <a href="#connectivity">{{ 'nav.connectivity'|trans({}, 'glossary') }}</a>
        <a href="#clusters">{{ 'nav.clusters'|trans({}, 'glossary') }}</a>
        <a href="#device-concepts">{{ 'nav.device_concepts'|trans({}, 'glossary') }}</a>
        <a href="#matter-spec">{{ 'nav.matter_spec'|trans({}, 'glossary') }}</a>
    </nav>

    <section class="glossary-section" id="connectivity">
        <h2>{{ 'connectivity.title'|trans({}, 'glossary') }}</h2>

        <div class="glossary-term" id="thread">
            <h3>{{ 'connectivity.thread.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'connectivity.thread.description_1'|trans({}, 'glossary') }}</p>
            <p>{{ 'connectivity.thread.description_2'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'connectivity.thread.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="wifi">
            <h3>{{ 'connectivity.wifi.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'connectivity.wifi.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'connectivity.wifi.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="ethernet">
            <h3>{{ 'connectivity.ethernet.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'connectivity.ethernet.description'|trans({}, 'glossary') }}</p>
        </div>

        <div class="glossary-term" id="border-router">
            <h3>{{ 'connectivity.border_router.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'connectivity.border_router.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'connectivity.border_router.example'|trans({}, 'glossary') }}
            </div>
        </div>
    </section>

    <section class="glossary-section" id="clusters">
        <h2>{{ 'clusters.title'|trans({}, 'glossary') }}</h2>

        <div class="glossary-term" id="cluster">
            <h3>{{ 'clusters.cluster.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'clusters.cluster.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'clusters.cluster.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="server-cluster">
            <h3>{{ 'clusters.server_cluster.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'clusters.server_cluster.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'clusters.server_cluster.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="client-cluster">
            <h3>{{ 'clusters.client_cluster.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'clusters.client_cluster.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'clusters.client_cluster.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="common-clusters">
            <h3>{{ 'clusters.common_clusters.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'clusters.common_clusters.description'|trans({}, 'glossary') }}</p>
            <p>
                <strong>On/Off</strong> <code>0x0006</code> &mdash; {{ 'clusters.common_clusters.onoff'|trans({}, 'glossary') }}<br>
                <strong>Level Control</strong> <code>0x0008</code> &mdash; {{ 'clusters.common_clusters.level_control'|trans({}, 'glossary') }}<br>
                <strong>Color Control</strong> <code>0x0300</code> &mdash; {{ 'clusters.common_clusters.color_control'|trans({}, 'glossary') }}<br>
                <strong>Temperature Measurement</strong> <code>0x0402</code> &mdash; {{ 'clusters.common_clusters.temperature_measurement'|trans({}, 'glossary') }}<br>
                <strong>Occupancy Sensing</strong> <code>0x0406</code> &mdash; {{ 'clusters.common_clusters.occupancy_sensing'|trans({}, 'glossary') }}<br>
                <strong>Door Lock</strong> <code>0x0101</code> &mdash; {{ 'clusters.common_clusters.door_lock'|trans({}, 'glossary') }}<br>
                <strong>Thermostat</strong> <code>0x0201</code> &mdash; {{ 'clusters.common_clusters.thermostat'|trans({}, 'glossary') }}
            </p>
        </div>
    </section>

    <section class="glossary-section" id="device-concepts">
        <h2>{{ 'device_concepts.title'|trans({}, 'glossary') }}</h2>

        <div class="glossary-term" id="binding">
            <h3>{{ 'device_concepts.binding.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'device_concepts.binding.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'device_concepts.binding.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="endpoint">
            <h3>{{ 'device_concepts.endpoint.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'device_concepts.endpoint.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'device_concepts.endpoint.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="device-type">
            <h3>{{ 'device_concepts.device_type.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'device_concepts.device_type.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.examples'|trans({}, 'messages') }}:</strong> {{ 'device_concepts.device_type.examples'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="vendor-id">
            <h3>{{ 'device_concepts.vendor_id.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'device_concepts.vendor_id.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.examples'|trans({}, 'messages') }}:</strong> {{ 'device_concepts.vendor_id.examples'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="product-id">
            <h3>{{ 'device_concepts.product_id.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'device_concepts.product_id.description'|trans({}, 'glossary') }}</p>
        </div>
    </section>

    <section class="glossary-section" id="matter-spec">
        <h2>{{ 'matter_spec.title'|trans({}, 'glossary') }}</h2>

        <div class="glossary-term" id="matter-version">
            <h3>{{ 'matter_spec.matter_version.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'matter_spec.matter_version.description'|trans({}, 'glossary') }}</p>
            <div class="example">
                <strong>{{ 'common.example'|trans({}, 'messages') }}:</strong> {{ 'matter_spec.matter_version.example'|trans({}, 'glossary') }}
            </div>
        </div>

        <div class="glossary-term" id="mandatory-cluster">
            <h3>{{ 'matter_spec.mandatory_cluster.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'matter_spec.mandatory_cluster.description'|trans({}, 'glossary') }}</p>
        </div>

        <div class="glossary-term" id="optional-cluster">
            <h3>{{ 'matter_spec.optional_cluster.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'matter_spec.optional_cluster.description'|trans({}, 'glossary') }}</p>
        </div>

        <div class="glossary-term" id="dcl">
            <h3>{{ 'matter_spec.dcl.name'|trans({}, 'glossary') }}</h3>
            <p>{{ 'matter_spec.dcl.description'|trans({}, 'glossary') }}</p>
        </div>
    </section>

    <div class="back-to-top">
        <a href="#top">&uarr; {{ 'back_to_top'|trans({}, 'glossary') }}</a>
    </div>
</div>
{% endblock %}
