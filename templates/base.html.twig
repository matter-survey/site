<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Matter Survey{% endblock %}</title>

    {# OpenGraph meta tags #}
    <meta property="og:site_name" content="Matter Survey">
    {% block og_meta %}
    <meta property="og:title" content="{% block og_title %}Matter Survey{% endblock %}">
    <meta property="og:description" content="{% block og_description %}Community-driven database of Matter smart home device capabilities{% endblock %}">
    <meta property="og:type" content="{% block og_type %}website{% endblock %}">
    {% endblock %}

    {# Twitter Card meta tags #}
    <meta name="twitter:card" content="summary">
    {% block twitter_meta %}
    <meta name="twitter:title" content="{{ block('og_title') }}">
    <meta name="twitter:description" content="{{ block('og_description') }}">
    {% endblock %}

    {# Standard meta description #}
    <meta name="description" content="{{ block('og_description') }}">

    {# JSON-LD Structured Data #}
    {% block structured_data %}{% endblock %}

    <style>
        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --gray-50: #f9fafb;
            --gray-100: #f3f4f6;
            --gray-200: #e5e7eb;
            --gray-300: #d1d5db;
            --gray-500: #6b7280;
            --gray-700: #374151;
            --gray-900: #111827;
            --green-100: #dcfce7;
            --green-700: #15803d;
            --blue-100: #dbeafe;
            --blue-700: #1d4ed8;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: var(--gray-900);
            background: var(--gray-50);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        header {
            background: white;
            border-bottom: 1px solid var(--gray-200);
            padding: 1rem 0;
            margin-bottom: 2rem;
        }

        header .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        header h1 {
            font-size: 1.5rem;
            font-weight: 600;
        }

        header h1 a {
            color: var(--gray-900);
            text-decoration: none;
        }

        header p {
            color: var(--gray-500);
            font-size: 0.875rem;
        }

        header nav {
            display: flex;
            gap: 0.5rem;
            align-items: center;
        }

        header nav a {
            padding: 0.375rem 0.75rem;
            font-size: 0.875rem;
            color: var(--gray-600);
            text-decoration: none;
            border-radius: 0.25rem;
        }

        header nav a:hover {
            background: var(--gray-100);
            color: var(--gray-900);
        }

        .header-search {
            display: flex;
            align-items: center;
        }

        .header-search input {
            padding: 0.375rem 0.75rem;
            font-size: 0.875rem;
            border: 1px solid var(--gray-300);
            border-radius: 0.375rem;
            width: 180px;
            outline: none;
        }

        .header-search input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.1);
        }

        .header-search button {
            padding: 0.375rem 0.625rem;
            margin-left: 0.25rem;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 0.375rem;
            cursor: pointer;
            font-size: 0.875rem;
        }

        .header-search button:hover {
            background: var(--primary-dark);
        }

        .nav-divider {
            width: 1px;
            height: 1.25rem;
            background: var(--gray-300);
            margin: 0 0.25rem;
        }

        .badge {
            display: inline-block;
            padding: 0.125rem 0.5rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 500;
        }

        .badge-binding {
            background: var(--green-100);
            color: var(--green-700);
        }

        .badge-thread {
            background: #dcfce7;
            color: #166534;
        }

        .badge-wifi {
            background: #dbeafe;
            color: #1e40af;
        }

        .badge-ethernet {
            background: #f3f4f6;
            color: #374151;
        }

        .badge-endpoints {
            background: var(--gray-100);
            color: var(--gray-700);
        }

        footer {
            text-align: center;
            padding: 2rem;
            color: var(--gray-500);
            font-size: 0.875rem;
        }

        footer a {
            color: var(--primary);
        }

        {% block extra_styles %}{% endblock %}
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <div>
                    <h1><a href="{{ path('device_index') }}">Matter Survey</a></h1>
                    {% block header_subtitle %}<p>Community-driven database of Matter device capabilities</p>{% endblock %}
                </div>
                <nav>
                    <form class="header-search" action="{{ path('device_index') }}" method="get">
                        <input type="text" name="q" placeholder="Search devices..." value="{{ app.request.query.get('q', '') }}" aria-label="Search devices">
                        <button type="submit" aria-label="Search">&#128269;</button>
                    </form>
                    <span class="nav-divider"></span>
                    <a href="{{ path('device_index') }}">Devices</a>
                    <a href="{{ path('vendor_index') }}">Vendors</a>
                    <a href="{{ path('stats_dashboard') }}">Stats</a>
                    <span class="nav-divider"></span>
                    <a href="{{ path('page_about') }}">About</a>
                    <a href="{{ path('page_faq') }}">FAQ</a>
                </nav>
            </div>
        </div>
    </header>

    <main class="container">
        {% block body %}{% endblock %}
    </main>

    <footer>
        <p>
            <a href="{{ path('page_about') }}">About</a> &middot;
            <a href="{{ path('page_faq') }}">FAQ</a> &middot;
            <a href="{{ path('page_glossary') }}">Glossary</a>
        </p>
        <p>
            Data contributed by <a href="https://github.com/cedricziel/ha-matter-binding-helper">Matter Binding Helper</a> users.
            <br>
            Privacy-focused: No personal data is collected.
        </p>
    </footer>
</body>
</html>
