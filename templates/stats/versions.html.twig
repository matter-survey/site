{% extends 'base.html.twig' %}

{% block title %}Matter Survey - Version Landscape{% endblock %}

{% block header_subtitle %}<p>Firmware and hardware version tracking across Matter devices</p>{% endblock %}

{% block extra_styles %}
    .dashboard-nav {
        display: flex;
        gap: 0.5rem;
        margin-bottom: 2rem;
        flex-wrap: wrap;
    }

    .dashboard-nav a {
        padding: 0.5rem 1rem;
        background: white;
        border: 1px solid var(--gray-200);
        border-radius: 0.375rem;
        text-decoration: none;
        color: var(--gray-700);
        font-size: 0.875rem;
    }

    .dashboard-nav a:hover {
        background: var(--gray-100);
    }

    .dashboard-nav a.active {
        background: var(--primary);
        color: white;
        border-color: var(--primary);
    }

    .stats-row {
        display: flex;
        gap: 1rem;
        margin-bottom: 2rem;
        flex-wrap: wrap;
    }

    .stat-card {
        background: white;
        padding: 1rem 1.5rem;
        border-radius: 0.5rem;
        border: 1px solid var(--gray-200);
    }

    .stat-card .value {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--primary);
    }

    .stat-card .label {
        font-size: 0.75rem;
        color: var(--gray-500);
    }

    .info-box {
        background: var(--blue-100);
        border-radius: 0.5rem;
        padding: 1rem 1.25rem;
        margin-bottom: 2rem;
        font-size: 0.875rem;
        color: var(--blue-700);
    }

    .section {
        background: white;
        border-radius: 0.5rem;
        border: 1px solid var(--gray-200);
        margin-bottom: 2rem;
    }

    .section-header {
        padding: 1rem 1.25rem;
        border-bottom: 1px solid var(--gray-200);
    }

    .section-header h2 {
        font-size: 1rem;
        font-weight: 600;
        margin: 0;
    }

    .section-header p {
        font-size: 0.75rem;
        color: var(--gray-500);
        margin: 0.25rem 0 0;
    }

    .version-table {
        width: 100%;
        border-collapse: collapse;
    }

    .version-table th,
    .version-table td {
        padding: 0.75rem 1.25rem;
        text-align: left;
        border-bottom: 1px solid var(--gray-100);
    }

    .version-table th {
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--gray-500);
        text-transform: uppercase;
    }

    .version-table tr:last-child td {
        border-bottom: none;
    }

    .version-table tr:hover td {
        background: var(--gray-50);
    }

    .version-table a {
        color: var(--gray-900);
        text-decoration: none;
        font-weight: 500;
    }

    .version-table a:hover {
        color: var(--primary);
    }

    .version-count {
        display: inline-flex;
        align-items: center;
        gap: 0.25rem;
    }

    .version-count .number {
        font-weight: 600;
        color: var(--primary);
    }

    .version-list {
        font-size: 0.75rem;
        color: var(--gray-500);
        margin-top: 0.25rem;
        max-width: 300px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .badge {
        display: inline-block;
        padding: 0.125rem 0.5rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 500;
    }

    .badge-update {
        background: #fef3c7;
        color: #b45309;
    }

    .badge-multi-hw {
        background: #fce7f3;
        color: #be185d;
    }

    .empty-state {
        padding: 3rem;
        text-align: center;
        color: var(--gray-500);
    }
{% endblock %}

{% block body %}
    <div class="dashboard-nav">
        <a href="{{ path('stats_dashboard') }}">Overview</a>
        <a href="{{ path('stats_clusters') }}">Clusters</a>
        <a href="{{ path('stats_device_types') }}">Device Types</a>
        <a href="{{ path('stats_binding') }}">Binding</a>
        <a href="{{ path('stats_versions') }}" class="active">Versions</a>
        <a href="{{ path('stats_pairings') }}">Pairings</a>
        <a href="{{ path('device_index') }}">Browse Devices</a>
        <a href="{{ path('vendor_index') }}">Vendors</a>
    </div>

    <div class="info-box">
        Devices with multiple software versions likely receive over-the-air (OTA) updates.
        Multiple hardware versions may indicate product revisions or regional variants.
    </div>

    <div class="stats-row">
        <div class="stat-card">
            <div class="value">{{ versionStats.products_with_versions|number_format }}</div>
            <div class="label">Products with Version Info</div>
        </div>
        <div class="stat-card">
            <div class="value">{{ versionStats.unique_software_versions|number_format }}</div>
            <div class="label">Unique Software Versions</div>
        </div>
        <div class="stat-card">
            <div class="value">{{ versionStats.unique_hardware_versions|number_format }}</div>
            <div class="label">Unique Hardware Versions</div>
        </div>
        <div class="stat-card">
            <div class="value">{{ productsWithMultipleVersions|length }}</div>
            <div class="label">Products with Updates</div>
        </div>
    </div>

    <div class="section">
        <div class="section-header">
            <h2>Products with Multiple Versions</h2>
            <p>Devices that have been seen with different software or hardware versions (indicating OTA activity or product revisions)</p>
        </div>
        {% if productsWithMultipleVersions|length > 0 %}
            <table class="version-table">
                <thead>
                    <tr>
                        <th>Product</th>
                        <th>Vendor</th>
                        <th>SW Versions</th>
                        <th>HW Versions</th>
                        <th>Software Version List</th>
                    </tr>
                </thead>
                <tbody>
                    {% for product in productsWithMultipleVersions %}
                        <tr>
                            <td>
                                <a href="{{ path('device_show', {slug: product.slug}) }}">
                                    {{ product.product_name ?? 'Unknown Product' }}
                                </a>
                            </td>
                            <td>
                                {% if product.vendor_slug %}
                                    <a href="{{ path('vendor_show', {slug: product.vendor_slug}) }}" style="font-weight: normal;">
                                        {{ product.vendor_name ?? 'Unknown' }}
                                    </a>
                                {% else %}
                                    {{ product.vendor_name ?? 'Unknown' }}
                                {% endif %}
                            </td>
                            <td>
                                <span class="version-count">
                                    <span class="number">{{ product.software_version_count }}</span>
                                    {% if product.software_version_count > 1 %}
                                        <span class="badge badge-update">OTA</span>
                                    {% endif %}
                                </span>
                            </td>
                            <td>
                                <span class="version-count">
                                    <span class="number">{{ product.hardware_version_count }}</span>
                                    {% if product.hardware_version_count > 1 %}
                                        <span class="badge badge-multi-hw">Revisions</span>
                                    {% endif %}
                                </span>
                            </td>
                            <td>
                                <div class="version-list" title="{{ product.software_versions }}">
                                    {{ product.software_versions|default('N/A') }}
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <div class="empty-state">
                <p>No products with multiple versions found yet.</p>
                <p style="font-size: 0.875rem;">As more data is collected, products with OTA updates will appear here.</p>
            </div>
        {% endif %}
    </div>
{% endblock %}
