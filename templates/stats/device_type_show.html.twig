{% extends 'base.html.twig' %}

{% block title %}{{ deviceType.name }} - Matter Survey{% endblock %}

{% block og_title %}{{ deviceType.name }} - Matter Device Type{% endblock %}
{% block og_description %}{{ deviceType.description ?? deviceType.name ~ ' is a Matter device type' }}. {{ totalDevices }} device{{ totalDevices != 1 ? 's' : '' }} implement{{ totalDevices == 1 ? 's' : '' }} this type. Spec version: {{ deviceType.specVersion ?? '1.0' }}.{% endblock %}

{% block structured_data %}
{% if deviceTypeEntity %}
<script type="application/ld+json">
{{ structured_data_device_type(deviceTypeEntity, totalDevices, aeoDateModified)|json_encode(constant('JSON_UNESCAPED_SLASHES'))|raw }}
</script>
<script type="application/ld+json">
{{ structured_data_breadcrumb(aeoBreadcrumbs)|json_encode(constant('JSON_UNESCAPED_SLASHES'))|raw }}
</script>
{% endif %}
{% endblock %}

{% block extra_styles %}
    .breadcrumb {
        margin-bottom: 1rem;
        font-size: 0.875rem;
    }

    .breadcrumb a {
        color: var(--primary);
        text-decoration: none;
    }

    .breadcrumb a:hover {
        text-decoration: underline;
    }

    .device-type-header {
        background: white;
        border-radius: 0.5rem;
        border: 1px solid var(--gray-200);
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .device-type-header-content {
        display: flex;
        gap: 1rem;
        align-items: flex-start;
    }

    .device-type-header-icon {
        width: 56px;
        height: 56px;
        background: var(--gray-100);
        border-radius: 0.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.75rem;
        flex-shrink: 0;
    }

    .device-type-header-info {
        flex: 1;
    }

    .device-type-header h1 {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 0.25rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .device-type-header .description {
        color: var(--gray-600);
        font-size: 0.9375rem;
        margin-bottom: 0.75rem;
    }

    .device-type-header .meta {
        color: var(--gray-500);
        font-size: 0.875rem;
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .spec-badge {
        display: inline-block;
        padding: 0.125rem 0.5rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 500;
    }

    .spec-1-0 { background: #dbeafe; color: #1d4ed8; }
    .spec-1-2 { background: #dcfce7; color: #15803d; }
    .spec-1-3 { background: #fef3c7; color: #b45309; }
    .spec-1-4 { background: #fce7f3; color: #be185d; }

    .category-tag {
        display: inline-block;
        padding: 0.125rem 0.5rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 500;
        background: var(--gray-100);
        color: var(--gray-600);
    }

    .category-tag-lights { background: #fef3c7; color: #b45309; }
    .category-tag-climate { background: #dbeafe; color: #1d4ed8; }
    .category-tag-sensors { background: #dcfce7; color: #15803d; }
    .category-tag-security { background: #fee2e2; color: #dc2626; }
    .category-tag-appliances { background: #ede9fe; color: #7c3aed; }
    .category-tag-entertainment { background: #fce7f3; color: #be185d; }
    .category-tag-energy { background: #ffedd5; color: #c2410c; }
    .category-tag-system { background: #f3f4f6; color: #4b5563; }

    .stats-row {
        display: flex;
        gap: 1rem;
        margin-bottom: 1.5rem;
        flex-wrap: wrap;
    }

    .stat-card {
        background: white;
        padding: 1rem 1.5rem;
        border-radius: 0.5rem;
        border: 1px solid var(--gray-200);
    }

    .stat-card .value {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--primary);
    }

    .stat-card .label {
        font-size: 0.75rem;
        color: var(--gray-500);
    }

    .device-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 1rem;
        margin-bottom: 2rem;
    }

    .device-card {
        background: white;
        border-radius: 0.5rem;
        border: 1px solid var(--gray-200);
        padding: 1rem;
        transition: border-color 0.15s;
    }

    .device-card:hover {
        border-color: var(--primary);
    }

    .device-card a {
        text-decoration: none;
        color: inherit;
    }

    .device-card h3 {
        font-size: 1rem;
        font-weight: 600;
        margin-bottom: 0.25rem;
        color: var(--gray-900);
    }

    .device-card .vendor {
        font-size: 0.875rem;
        color: var(--gray-500);
        margin-bottom: 0.5rem;
    }

    .device-card .device-meta {
        display: flex;
        gap: 0.75rem;
        font-size: 0.75rem;
        color: var(--gray-500);
        flex-wrap: wrap;
    }

    .device-card .badge {
        font-size: 0.7rem;
        padding: 0.125rem 0.375rem;
    }

    .pagination {
        display: flex;
        justify-content: center;
        gap: 0.5rem;
        margin: 2rem 0;
    }

    .pagination a, .pagination span {
        padding: 0.5rem 1rem;
        border: 1px solid var(--gray-300);
        border-radius: 0.375rem;
        text-decoration: none;
        color: var(--gray-700);
    }

    .pagination a:hover {
        background: var(--gray-100);
    }

    .pagination .current {
        background: var(--primary);
        color: white;
        border-color: var(--primary);
    }

    .empty-state {
        text-align: center;
        padding: 3rem;
        color: var(--gray-500);
        background: white;
        border-radius: 0.5rem;
        border: 1px solid var(--gray-200);
    }

    .cluster-section {
        background: white;
        border-radius: 0.5rem;
        border: 1px solid var(--gray-200);
        margin-bottom: 1.5rem;
    }

    .cluster-section-header {
        padding: 1rem 1.5rem;
        border-bottom: 1px solid var(--gray-200);
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .cluster-section-header h2 {
        font-size: 1.125rem;
        font-weight: 600;
        margin: 0;
    }

    .cluster-section-header .count {
        background: var(--gray-100);
        padding: 0.25rem 0.75rem;
        border-radius: 9999px;
        font-size: 0.875rem;
        color: var(--gray-600);
    }

    .cluster-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
        padding: 1.5rem;
    }

    @media (max-width: 768px) {
        .cluster-grid {
            grid-template-columns: 1fr;
        }
    }

    .cluster-card {
        background: var(--gray-50);
        border-radius: 0.375rem;
        padding: 1rem;
    }

    .cluster-card h3 {
        font-size: 0.875rem;
        font-weight: 600;
        color: var(--gray-700);
        margin-bottom: 0.5rem;
    }

    .cluster-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .cluster-list li {
        padding: 0.5rem 0;
        border-bottom: 1px solid var(--gray-200);
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.875rem;
    }

    .cluster-list li:last-child {
        border-bottom: none;
    }

    .cluster-list .cluster-name {
        color: var(--gray-900);
        font-weight: 500;
    }

    .cluster-list .cluster-id {
        color: var(--gray-500);
        font-size: 0.75rem;
        font-family: monospace;
    }

    .cluster-list a {
        text-decoration: none;
        color: inherit;
    }

    .cluster-list a:hover .cluster-name {
        color: var(--primary);
    }

    .empty-clusters {
        padding: 1.5rem;
        text-align: center;
        color: var(--gray-500);
        font-size: 0.875rem;
    }

    .mandatory-badge {
        display: inline-block;
        padding: 0.125rem 0.5rem;
        border-radius: 9999px;
        font-size: 0.625rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .mandatory-badge.required {
        background: #fee2e2;
        color: #dc2626;
    }

    .mandatory-badge.optional {
        background: #dbeafe;
        color: #1d4ed8;
    }

    .section-title {
        font-size: 1.25rem;
        font-weight: 600;
        margin: 2rem 0 1rem;
        color: var(--gray-900);
    }

    .section-header-with-controls {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .section-header-with-controls .section-title {
        margin: 0;
    }

    .sort-controls {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .sort-label {
        font-size: 0.875rem;
        color: var(--gray-500);
    }

    .sort-option {
        font-size: 0.875rem;
        padding: 0.25rem 0.75rem;
        border-radius: 9999px;
        text-decoration: none;
        color: var(--gray-600);
        background: var(--gray-100);
        transition: all 0.15s;
    }

    .sort-option:hover {
        background: var(--gray-200);
    }

    .sort-option.active {
        background: var(--primary);
        color: white;
    }

    .device-card-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 0.5rem;
    }

    .device-card-header h3 {
        flex: 1;
        min-width: 0;
    }

    /* Star rating badge styles */
    .badge-rating {
        display: inline-flex;
        align-items: center;
        gap: 0.125rem;
        padding: 0.125rem 0.5rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 600;
        white-space: nowrap;
        flex-shrink: 0;
    }

    /* Badge rating colors (rating * 10) */
    .badge-rating-50 { background: #dcfce7; color: #15803d; }
    .badge-rating-45 { background: #d1fae5; color: #15803d; }
    .badge-rating-40 { background: #dbeafe; color: #1d4ed8; }
    .badge-rating-35 { background: #e0f2fe; color: #1d4ed8; }
    .badge-rating-30 { background: #fef3c7; color: #b45309; }
    .badge-rating-25 { background: #fef9c3; color: #b45309; }
    .badge-rating-20 { background: #fed7aa; color: #c2410c; }
    .badge-rating-15 { background: #fee2e2; color: #dc2626; }
    .badge-rating-10 { background: #fee2e2; color: #dc2626; }
    .badge-rating-5 { background: #fee2e2; color: #dc2626; }

    .badge-rating-noncompliant {
        position: relative;
    }

    .badge-rating .compliance-warning {
        color: #dc2626;
        font-weight: 700;
        margin-left: 0.125rem;
    }

    .icon-lightbulb::before { content: '💡'; }
    .icon-thermometer::before { content: '🌡️'; }
    .icon-lock::before { content: '🔒'; }
    .icon-plug::before { content: '🔌'; }
    .icon-fan::before { content: '🌀'; }
    .icon-tv::before { content: '📺'; }
    .icon-volume-up::before { content: '🔊'; }
    .icon-robot::before { content: '🤖'; }
    .icon-server::before { content: '🖥️'; }
    .icon-hub::before { content: '🔗'; }
    .icon-bridge::before { content: '🌉'; }
    .icon-toggle-on::before { content: '🔘'; }
    .icon-door-open::before { content: '🚪'; }
    .icon-blinds::before { content: '🪟'; }
    .icon-sun::before { content: '☀️'; }
    .icon-droplet::before { content: '💧'; }
    .icon-wind::before { content: '💨'; }
    .icon-bolt::before { content: '⚡'; }
    .icon-fire::before { content: '🔥'; }
    .icon-snowflake::before { content: '❄️'; }
    .icon-bell::before { content: '🔔'; }
    .icon-water::before { content: '🚰'; }
    .icon-gauge::before { content: '📊'; }
    .icon-leaf::before { content: '🌿'; }
    .icon-palette::before { content: '🎨'; }
    .icon-sliders::before { content: '🎚️'; }
    .icon-battery::before { content: '🔋'; }
    .icon-battery-full::before { content: '🔋'; }
    .icon-charging-station::before { content: '⚡'; }
    .icon-solar-panel::before { content: '☀️'; }
    .icon-router::before { content: '📡'; }
    .icon-download::before { content: '⬇️'; }
    .icon-upload::before { content: '⬆️'; }
    .icon-device::before { content: '📱'; }
    .icon-faucet::before { content: '🚿'; }
    .icon-motion-sensor::before { content: '👁️'; }
    .icon-washing-machine::before { content: '🧺'; }
    .icon-utensils::before { content: '🍽️'; }
    .icon-microwave::before { content: '📻'; }
    .icon-box::before { content: '📦'; }
    .icon-cloud-rain::before { content: '🌧️'; }
    .icon-cast::before { content: '📡'; }
    .icon-remote::before { content: '🎮'; }
    .icon-play-circle::before { content: '▶️'; }
    .icon-network-wired::before { content: '🔌'; }
{% endblock %}

{% from 'components/_star_rating.html.twig' import starsBadge %}

{% block body %}
    <div class="breadcrumb">
        <a href="{{ path('device_index') }}">Home</a>
        &rarr;
        <a href="{{ path('stats_device_types') }}">Device Types</a>
        &rarr;
        {{ deviceType.name }}
    </div>

    {% if deviceTypeEntity %}
        <p class="aeo-lede">{{ aeo_lede_device_type(deviceTypeEntity, totalDevices) }}</p>
        {% if aeoDateModified %}
            <p class="aeo-meta">
                <time datetime="{{ aeoDateModified.format('Y-m-d') }}">Last updated {{ aeoDateModified.format('j M Y') }}</time>
            </p>
        {% endif %}
    {% endif %}

    <div class="device-type-header">
        <div class="device-type-header-content">
            <div class="device-type-header-icon icon-{{ deviceType.icon }}"></div>
            <div class="device-type-header-info">
                <h1>
                    {{ deviceType.name }}
                    {% if deviceType.specVersion %}
                        <span class="spec-badge spec-{{ deviceType.specVersion|replace({'.': '-'}) }}">{{ deviceType.specVersion }}</span>
                    {% endif %}
                </h1>
                {% if deviceType.description %}
                    <div class="description">{{ deviceType.description }}</div>
                {% endif %}
                <div class="meta">
                    <span>Device Type ID: {{ deviceType.id }} ({{ deviceType.hexId|default('0x' ~ '%04X'|format(deviceType.id)) }})</span>
                    {% if deviceType.displayCategory %}
                        <span class="category-tag category-tag-{{ deviceType.displayCategory|lower|replace({' ': '-'}) }}">{{ deviceType.displayCategory }}</span>
                    {% endif %}
                    {% if deviceType.class %}
                        <span>Class: {{ deviceType.class }}</span>
                    {% endif %}
                    {% if deviceType.scope %}
                        <span>Scope: {{ deviceType.scope }}</span>
                    {% endif %}
                </div>
                {% if deviceType.superset %}
                    <div class="meta" style="margin-top: 0.5rem;">
                        <span>Extends: <strong>{{ deviceType.superset }}</strong></span>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>

    <div class="stats-row">
        <div class="stat-card">
            <div class="value">{{ totalDevices|number_format }}</div>
            <div class="label">Device{{ totalDevices != 1 ? 's' : '' }} Implementing</div>
        </div>
    </div>

    {% if mandatoryServerClusters|length > 0 or optionalServerClusters|length > 0 or mandatoryClientClusters|length > 0 or optionalClientClusters|length > 0 %}
        <h2 class="section-title">Cluster Requirements</h2>

        <div class="cluster-section">
            <div class="cluster-section-header">
                <h2>Server Clusters</h2>
                <span class="count">{{ mandatoryServerClusters|length + optionalServerClusters|length }} total</span>
            </div>
            {% if mandatoryServerClusters|length > 0 or optionalServerClusters|length > 0 %}
                <div class="cluster-grid">
                    <div class="cluster-card">
                        <h3>Mandatory <span class="mandatory-badge required">Required</span></h3>
                        {% if mandatoryServerClusters|length > 0 %}
                            <ul class="cluster-list">
                                {% for cluster in mandatoryServerClusters %}
                                    <li>
                                        <a href="{{ path('stats_cluster_show', {hexId: '0x%04X'|format(cluster.id)}) }}">
                                            <span class="cluster-name">{{ cluster.name }}</span>
                                            <span class="cluster-id">0x{{ '%04X'|format(cluster.id) }}</span>
                                        </a>
                                    </li>
                                {% endfor %}
                            </ul>
                        {% else %}
                            <p class="empty-clusters">None required</p>
                        {% endif %}
                    </div>
                    <div class="cluster-card">
                        <h3>Optional <span class="mandatory-badge optional">Optional</span></h3>
                        {% if optionalServerClusters|length > 0 %}
                            <ul class="cluster-list">
                                {% for cluster in optionalServerClusters %}
                                    <li>
                                        <a href="{{ path('stats_cluster_show', {hexId: '0x%04X'|format(cluster.id)}) }}">
                                            <span class="cluster-name">{{ cluster.name }}</span>
                                            <span class="cluster-id">0x{{ '%04X'|format(cluster.id) }}</span>
                                        </a>
                                    </li>
                                {% endfor %}
                            </ul>
                        {% else %}
                            <p class="empty-clusters">None specified</p>
                        {% endif %}
                    </div>
                </div>
            {% else %}
                <div class="empty-clusters">No server clusters specified for this device type.</div>
            {% endif %}
        </div>

        <div class="cluster-section">
            <div class="cluster-section-header">
                <h2>Client Clusters</h2>
                <span class="count">{{ mandatoryClientClusters|length + optionalClientClusters|length }} total</span>
            </div>
            {% if mandatoryClientClusters|length > 0 or optionalClientClusters|length > 0 %}
                <div class="cluster-grid">
                    <div class="cluster-card">
                        <h3>Mandatory <span class="mandatory-badge required">Required</span></h3>
                        {% if mandatoryClientClusters|length > 0 %}
                            <ul class="cluster-list">
                                {% for cluster in mandatoryClientClusters %}
                                    <li>
                                        <a href="{{ path('stats_cluster_show', {hexId: '0x%04X'|format(cluster.id)}) }}">
                                            <span class="cluster-name">{{ cluster.name }}</span>
                                            <span class="cluster-id">0x{{ '%04X'|format(cluster.id) }}</span>
                                        </a>
                                    </li>
                                {% endfor %}
                            </ul>
                        {% else %}
                            <p class="empty-clusters">None required</p>
                        {% endif %}
                    </div>
                    <div class="cluster-card">
                        <h3>Optional <span class="mandatory-badge optional">Optional</span></h3>
                        {% if optionalClientClusters|length > 0 %}
                            <ul class="cluster-list">
                                {% for cluster in optionalClientClusters %}
                                    <li>
                                        <a href="{{ path('stats_cluster_show', {hexId: '0x%04X'|format(cluster.id)}) }}">
                                            <span class="cluster-name">{{ cluster.name }}</span>
                                            <span class="cluster-id">0x{{ '%04X'|format(cluster.id) }}</span>
                                        </a>
                                    </li>
                                {% endfor %}
                            </ul>
                        {% else %}
                            <p class="empty-clusters">None specified</p>
                        {% endif %}
                    </div>
                </div>
            {% else %}
                <div class="empty-clusters">No client clusters specified for this device type.</div>
            {% endif %}
        </div>
    {% endif %}

    <div class="section-header-with-controls">
        <h2 class="section-title">Devices Implementing This Type</h2>
        {% if devices is not empty %}
            <div class="sort-controls">
                <span class="sort-label">Sort by:</span>
                <a href="{{ path('stats_device_type_show', {type: deviceType.id, sort: 'rating'}) }}" class="sort-option{% if currentSort == 'rating' %} active{% endif %}">Rating</a>
                <a href="{{ path('stats_device_type_show', {type: deviceType.id, sort: 'name'}) }}" class="sort-option{% if currentSort == 'name' %} active{% endif %}">Name</a>
                <a href="{{ path('stats_device_type_show', {type: deviceType.id, sort: 'recent'}) }}" class="sort-option{% if currentSort == 'recent' %} active{% endif %}">Recent</a>
            </div>
        {% endif %}
    </div>

    {% if devices is empty %}
        <div class="empty-state">
            <p>No devices implementing this device type in the database yet.</p>
            <p style="font-size: 0.875rem; margin-top: 0.5rem;">Contribute by running the survey on your Matter devices!</p>
        </div>
    {% else %}
        <div class="device-grid">
            {% for device in devices %}
                {% set score = deviceScores[device.id] ?? null %}
                <div class="device-card">
                    <a href="{{ path('device_show', {slug: device.slug}) }}">
                        <div class="device-card-header">
                            <h3>{{ device.product_name ?? 'Unknown Product' }}</h3>
                            {% if score %}
                                {{ starsBadge(score.star_rating, score.overall_score, score.is_compliant) }}
                            {% endif %}
                        </div>
                        <div class="vendor">{{ device.vendor_name ?? 'Unknown Vendor' }}</div>
                        <div class="device-meta">
                            <span>Vendor: {{ device.vendor_id }}</span>
                            <span>Product: {{ device.product_id }}</span>
                            {% if device.supports_binding %}
                                <span class="badge badge-binding">Binding</span>
                            {% endif %}
                            <span>{{ device.endpoint_count }} endpoint{{ device.endpoint_count != 1 ? 's' : '' }}</span>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>

        {% if totalPages > 1 %}
            <div class="pagination">
                {% if currentPage > 1 %}
                    <a href="{{ path('stats_device_type_show', {type: deviceType.id, page: currentPage - 1, sort: currentSort}) }}">&laquo; Prev</a>
                {% endif %}

                {% for i in max(1, currentPage - 2)..min(totalPages, currentPage + 2) %}
                    {% if i == currentPage %}
                        <span class="current">{{ i }}</span>
                    {% else %}
                        <a href="{{ path('stats_device_type_show', {type: deviceType.id, page: i, sort: currentSort}) }}">{{ i }}</a>
                    {% endif %}
                {% endfor %}

                {% if currentPage < totalPages %}
                    <a href="{{ path('stats_device_type_show', {type: deviceType.id, page: currentPage + 1, sort: currentSort}) }}">Next &raquo;</a>
                {% endif %}
            </div>
        {% endif %}
    {% endif %}
{% endblock %}
